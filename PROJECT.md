# PROJECT.md — Stockholm

> 이 문서는 프로젝트의 목적, 확정된 아키텍처 결정, 기능 명세, 안전 규칙을 담는 **단일 기준 문서**다.
> 코드가 이 문서와 어긋나야 할 이유가 생기면, 코드를 바꾸기 전에 합의하고 이 문서를 먼저 고친다.
> 표기: **[확정]** 결정됨 / **[제안]** 제안 단계, 확인 전 / **[확인 필요]** 외부 사실 확인이 필요함

- 저장소: https://github.com/lyvius2/stockholm.git
- 최종 갱신: 2026-09-24

---

## 1. 목적과 성격

여러 인격(페르소나)의 AI 토론과 미러피시(MiroFish) 시뮬레이션 결과를 근거로 주식 매수·매도 의사결정을 지원하고, 토스증권 OpenAPI로 주문을 실행하는 **개인용 프로그램**이다.

- 성인 가족 단위의 폐쇄형 사용. 최대 사용자 **4명 고정**(admin 포함). [확정]
- 대상 시장: 국내 주식, 해외(미국) 주식. [확정]
- 예측 정확도를 보장하는 시스템이 아니다. 판단 근거를 구조화·기록하고, 자동 실행은 **코드로 강제되는 안전장치 안에서만** 일어난다.
- 이 프로젝트의 어떤 산출물도 투자 조언이 아니다.

## 2. 아키텍처 결정 요약

| ID | 결정 | 근거 |
|---|---|---|
| D1 | 클라이언트 = Electron(React+TypeScript) 셸 + **Java/Spring Boot 데몬** | 핵심 매매 로직을 주력 스택으로. Electron을 닫아도 엔진은 동작 |
| D2 | 서버는 **optional**. 역할: 원격 컨트롤, 그 UI 제공, 클라이언트 간 동기화 중계, Slack 알림 | 클라이언트는 서버 없이 완결 |
| D3 | Java 부분은 **단일 Spring Boot 프로젝트**(`backend/`). 클라이언트 데몬과 서버는 프로필(`engine` / `relay`)로 구분. 모듈 경계는 패키지 + Spring Modulith·ArchUnit 테스트로 강제 | 1인 유지보수. Mac mini에서는 한 JVM에 두 프로필을 함께 올림 |
| D4 | 모든 업무 데이터는 클라이언트에 저장. 서버는 **암호문만** 중계·임시보관 | 서버가 뚫려도 키·매매 데이터 비노출 |
| D5 | 각 클라이언트는 독립 동작(offline-first). 집 Mac이 꺼져도 밖의 Mac이 단독 동작 | 요구사항 |
| D6 | 자동 실행 권한은 한 시점에 한 디바이스만(**lease**). 갱신 실패 시 자동 실행 자진 중단 | 이중 주문(split-brain) 방지 |
| D7 | DB는 클라이언트·서버 모두 임베디드 **SQLite(WAL)**. 서버의 MySQL은 프로필 전환 옵션으로만 | 4인 규모, 단일 jar 배포, 영속성 코드 공유 |
| D8 | **매매는 토스증권 API만.** 타 증권사 API는 정보 수집 전용 | `TradingPort` 단일 구현 |
| D9 | 금융결제원 API는 본인인증과 본인 자산 조회에만 사용 (키 확보됨) | `IdentityPort`, `AssetPort` |
| D10 | 사용자별로 계좌·**개인 키(토스, 금융결제원 사용자 토큰)**·동기화 암호키 분리. admin 권한은 회원 관리와 공유 키 관리까지만 | 가족 간에도 타인 계좌·매매내역 접근 차단 |
| D11 | 인증 3계층: 사용자(비밀번호+TOTP/패스키), 디바이스(키쌍), 민감 명령 step-up | 원격에서 실제 돈이 움직임 |
| D12 | Slack은 **알림 전용**. 승인 버튼 없음. 사용자별 DM. 알림 전용 평문 경로 분리 | MFA 우회 경로 차단, 가족 간 프라이버시 |
| D13 | 미러피시는 선택적 컴포넌트, **관리형 설치** (2026-09-23 개정). dmg에는 넣지 않고, 사용자가 설정에서 "설치"를 누르면 **데몬이 `uv`·Python·고정 커밋의 미러피시 백엔드를 앱 데이터 폴더에 내려받아 설치하고, 심층 토론 요청 시에만 자식 프로세스로 띄웠다가 유휴 시 내린다.** 프론트엔드는 설치하지 않는다(우리 화면이 대신, 백엔드 API만 호출). 업데이트는 앱 릴리스에 박힌 검증 커밋으로만. 외부 주소 등록(다른 머신의 미러피시)도 계속 지원. 없으면 자체 토론만 | Python 런타임 번들·서명 부담과 dmg 크기, 상류 내부 API 변동, Zep Cloud 외부 의존(내장해도 자족 불가). AGPL은 개인 사용이라 제약 아님. 사전 실험으로 API 단독 구동·포트/경로 설정·Zep 한도 확인 후 확정 [확인 필요] |
| D14 | 단일 모노레포 → 단일 dmg (jlink 경량 JRE 번들) | 설치 단순화 |
| D15 | 자동화는 **모의 실행 → 승인 후 실행 → 완전 자동**의 단계적 전환 | 검증된 범위에서만 실제 자금 연결 |
| D16 | AI 학습 결과는 가족 간 공유(개인 매매 정보 제외). **가족 공유 키** 사용 | 표본 확보 |
| D17 | **키는 최초 구동 시 강제 입력, 이후에는 admin의 변경·삭제만.** 서비스 키(LLM·DART·뉴스·Slack 등)는 admin이 입력해 가족이 공유, 토스 키는 각 구성원이 본인 최초 로그인 때 입력. 필수는 LLM 1개 이상 + DART + 본인 토스 키. 키 값은 쓰기 전용(조회 불가). 예외: 개인 토스 키는 본인도 교체 가능. 공용 시세 수집은 admin의 토스 키, 주문·계좌 조회는 항상 본인 키. 개인 키 추가 암호화는 구성원별 옵션 | 입력 지점 최소화, 본인 계좌만 매매 |
| D18 | **가족은 같은 디바이스 하나를 함께 쓰는 것이 기본 전제.** 한 설치(데몬 하나)에 최대 4계정. 다른 디바이스를 쓰면 키를 포함한 데이터를 암호화해 서버 경유로 전달·동기화 | 요구사항 |
| D19 | **토론은 세 테마(매수·매도·산업 동향), 사용자가 요청할 때 시작, 같은 인격으로 재개 가능(정의가 바뀌었으면 새 정의).** 진행은 **메신저 UI로 실시간 표시**, 사용자는 도중에 질문·반박·동의·자료 추가·결론 요청으로 개입. 전망 요약은 주 1회 갱신. 구현은 **RAG + 토론 상태 저장**, 파인튜닝은 하지 않는다 | 최신성, 근거 추적, 다중 제공자, 표본 부족. 상세 `docs/DEBATE_DESIGN.md` |
| D21 | **차트·가격은 실시간.** 토스 WebSocket → 데몬 분봉 집계 → 로컬 WebSocket → 마지막 봉·현재가·호가 갱신. 끊기면 REST 폴링과 "지연" 표시 | 토스 앱 수준의 체감. 상세 `docs/DEBATE_DESIGN.md` 8장 |
| D20 | **메인 화면은 네 영역 고정**: 좌 차트/주문(7:3), 우 가격/토론(4:6), 좌우 6:4. 세 구분선 모두 마우스로 조절, 열끼리 독립. 비율은 사용자 설정(동기화) | 스케치 기반. 화면 설계 아티팩트 참조 |

모두 [확정].

## 3. 구성요소

**클라이언트 데몬 (`engine` 프로필).** 시스템의 두뇌. 데이터 수집, RAG, 페르소나 토론, 미러피시 호출, 주문 실행, 가드레일, 스케줄링, 동기화 에이전트, 알림 발신.
- `127.0.0.1`에만 바인딩. REST + WebSocket. 기동 시 생성한 로컬 토큰을 가진 프로세스만 접속 허용.
- 비밀값(토스·LLM·금융결제원 키, 디바이스 개인키, 동기화 키)은 DB가 아닌 **macOS Keychain**에 저장.
- 상주 메모리 목표 **400MB 이하**(`-Xmx384m` 기준). 1단계에서 실측. 네이티브 이미지 방침은 11.2 참조.

**Electron 셸 (`desktop/`).** 화면만 담당. 데몬을 기동·접속하고, 창을 닫으면 메뉴바 트레이로 상주.

**웹 UI.** React 앱 하나를 두 모드로: 로컬 모드(Electron이 로드, 로컬 데몬과 통신) / 원격 모드(서버가 서빙, 서버 경유로 대상 클라이언트와 통신). 데이터 접근 계층만 추상화한다. 화면을 두 번 만들지 않는다.

**서버 (`relay` 프로필).** 회원·디바이스 관리, 인증, Command/Event 중계, 암호화된 동기화 이벤트의 store-and-forward, lease 관리, 원격 웹 UI 서빙, Slack 발송.
- 업무 로직 없음. 증권·LLM 키 보관 안 함. 주문 직접 실행 안 함.
- 배치: 집 Mac mini(DDNS, Tailscale/Cloudflare Tunnel 우선) 또는 외부 소형 VPS. D5를 온전히 살리려면 VPS.

## 4. 운영 모드

1. **단독** — 서버 없음. 한 디바이스에 최대 4명의 로컬 계정(비밀번호 + TOTP). lease 규칙 꺼짐. **기본 사용 형태.**
2. **원격 컨트롤** — 서버에 연결된 클라이언트를 밖에서 웹으로 조작.
3. **멀티 클라이언트** — 같은 사용자의 여러 디바이스가 서버 경유로 동기화, 자동 실행 권한 handover.

구현은 이 순서. 단, **단독 모드 때부터 동기화 가능한 이벤트 로그 구조로 저장**한다.

## 5. 사용자와 인증

- **사용자 등록부는 클라이언트(설치)에 있다.** 최대 4명을 클라이언트에서 강제하고, relay 서버에 연결하면 등록부를 서버에 반영한다(서버도 4명 제한을 검증). 공개 가입 없음. 최초 구동한 사람이 admin이 되고, 구성원은 admin이 발급한 1회용 **등록 코드**로만 가입.
- 최초 구동 마법사, 키 분류, 변경 규칙, 다른 디바이스로의 키 전달은 `docs/KEY_MANAGEMENT.md`.
- 클라이언트의 로컬 계정을 서버에 "등록"하는 방식으로 연결(서버를 나중에 붙여도 데이터가 이어짐).
- 사용자 인증: 비밀번호(Argon2id) + TOTP 기본, 패스키 선택. 메일 인증번호는 가입 확인·새 디바이스 등록·계정 복구의 **보조 수단**. 메일 발송은 외부 SMTP 설정.
- 디바이스 인증: 설치 시 키쌍 생성, 공개키 등록, 접속 시 챌린지 서명. 세션 토큰은 디바이스에 바인딩. 디바이스 개별 폐기 가능.
- step-up: 주문 승인, 가드레일·자동화 설정 변경, 디바이스 등록, lease 강제 인수는 직전 TOTP 재인증.
- 모든 명령에 nonce + 만료시각. 전 과정 감사 로그. 로그인 시도 제한과 잠금.
- 권한 경계: 사용자 A의 데이터는 A의 디바이스에서만 복호화. admin은 초대·정지·디바이스 폐기만. 각 사용자는 **본인 명의 계좌를 본인 키로만** 매매.

## 6. 데이터와 동기화

**저장소.** SQLite(WAL) + JPA/Hibernate + Flyway. 벤더 종속 SQL 금지(MySQL 전환 가능성 유지). RAG 벡터·전문 검색은 **Apache Lucene**(HNSW + Nori BM25 하이브리드), 임베딩 기본값은 로컬 Ollama `bge-m3`. 원문은 SQLite, 인덱스는 재생성 가능한 파생물. **모든 검색은 기준 시점 `asOf`를 필수로 받는다.** 숫자(시세·재무·보유)는 RAG가 아니라 구조화 데이터로 전달한다. 상세는 `docs/RAG_DESIGN.md` [제안].

**금액.** 전 구간 `BigDecimal` + 통화 코드. `double`/`float` 금지.

**이벤트 로그.** 동기화 대상 데이터의 변경은 모두 추가 전용 이벤트: `(userId, deviceId, seq, occurredAt, type, payload)`. 화면용 테이블은 이벤트를 적용한 결과물(projection). 동기화는 디바이스별 수신 위치를 교환하고 빠진 구간만 전송. 서버는 미수신분을 암호문으로 보관 후 전달.

| 구분 | 대상 |
|---|---|
| 동기화함 | 관심종목, 페르소나 정의, 토론 결과·리포트, 매매 일지, 설정, 가드레일·자동화 설정, 학습 기록 |
| 동기화 안 함 | 잔고·보유·체결(원본은 증권사 → 각자 API 조회), RAG 인덱스(각자 구축) |
| 키 전달 | 공유 키는 가족 공유 키로, 개인 키는 사용자 동기화 키로 암호화해 전달. 새 디바이스 추가 시에는 **기존 디바이스가 승인하고 새 디바이스 공개키로 감싸서** 보낸다(서버는 암호문만 중계) |

**충돌.** 추가형은 충돌 없음. 수정형은 항목 단위 last-write-wins. **가드레일·자동화 한도 값은 충돌 시 더 보수적인 값**을 택하고 사용자에게 알림.

**암호화.** 사용자별 동기화 키(본인 디바이스 간) + 가족 공유 키(학습 공유용). 둘 다 디바이스 등록 시 디바이스 공개키로 감싸 전달. 서버는 봉투(라우팅 메타데이터)만 읽는다. 회원 탈퇴·정지 시 가족 공유 키 교체.

**lease.** 동기화가 켜진 사용자는 서버를 통해 자동 실행 lease를 주기적으로 갱신. 실패하면 자동 실행을 스스로 멈추고 수동 기능만 유지. 서버에 닿지 않을 때 다른 디바이스에서 자동 실행을 켜려면 경고 확인 후 **수동 강제 인수**. 최종 방어선: 자동 주문 직전 항상 토스 API로 미체결·당일 체결을 조회해 같은 의도의 주문이 있는지 확인.

## 7. 통신 프로토콜

클라이언트가 서버로 **아웃바운드 WebSocket(TLS)** 을 맺는다. 공통 봉투:

```json
{
  "id": "uuid",
  "kind": "COMMAND | EVENT | QUERY | SNAPSHOT | SYNC | NOTIFY | LEASE",
  "userId": "u_...",
  "fromDevice": "d_...",
  "toDevice": "d_... | null",
  "issuedAt": "ISO-8601 UTC",
  "expiresAt": "ISO-8601 UTC",
  "nonce": "...",
  "stepUpProof": "민감 명령에만",
  "body": "kind에 따라 평문 JSON 또는 암호문"
}
```

- `COMMAND` 원격 웹 → 클라이언트. **검증과 실행은 클라이언트가 한다.**
- `EVENT` 클라이언트의 상태 변화(토론 완료, 체결, 가드레일 발동).
- `QUERY`/`SNAPSHOT` 원격 화면용 상태 조회.
- `SYNC` 이벤트 로그 교환. 본문은 암호문.
- `NOTIFY` Slack용. 서버가 읽을 수 있는 **최소 정보**만. 수량·금액 포함 여부는 사용자 설정.
- `LEASE` 실행 권한 획득·갱신·반납.

스키마는 `protocol/`에 JSON Schema로 두고 Java·TypeScript 타입을 생성한다. 손으로 양쪽 타입을 따로 쓰지 않는다.

## 8. 외부 연동 (포트)

| 대상 | 용도 | 포트 | 비고 |
|---|---|---|---|
| 토스증권 OpenAPI | 시세, 주문, 조건주문, 계좌, 랭킹, 종목 경고, 시장 달력, 환율, WebSocket (국내·미국) | `TradingPort`, `MarketDataPort`, `MarketCalendarPort`, `RealtimeFeedPort` | **유일한 주문 경로.** `clientOrderId` 멱등성·WebSocket·애프터마켓 주문 확인됨. **허용 IP 사전 등록, 클라이언트당 토큰 1개** 제약 있음 → 8.1 |
| 타 증권사 API (신한 등) | 정보 수집 전용 | `MarketDataPort` | 주문 기능 구현 금지 |
| 금융결제원 API | 본인인증, 본인 자산 조회 | `IdentityPort`, `AssetPort` | [확인 필요] 테스트베드 키/운영 키 여부 |
| DART OpenAPI | 공시 | `DisclosurePort` | 1순위 데이터 소스 |
| SEC EDGAR 13F-HR (국민연금 CIK 0001608046) · 공공데이터포털 국민연금공단 해외주식 투자정보 | F13 분기 미국 상장분(주식 수·평가액) · 연간 전 지역(평가액·비중·지분율), 갱신 감지 | `PensionHoldingsPort` | 13F 분기 4회 + 연간 1회. 13F는 CUSIP, 연간은 회사명만 → 종목 마스터 매칭. 상세 `docs/NPS_HOLDINGS_DESIGN.md` |
| 뉴스 등 | 네이버 검색 API(뉴스·블로그·카페), RSS. 크롤링은 보조 | `NewsPort` | 발행 시각 메타데이터 필수(백테스트 시 미래 정보 혼입 방지). 네이버 API 약관 위험은 EXTERNAL_APIS 2.7 |
| 커뮤니티 (StockTwits 공개 API) | F12 미국 종목 여론 피드 | `CommunityPort` | 공식 API가 있는 소스만. Reddit은 사전 승인·48시간 삭제 규칙 때문에 링크아웃 전용 [보류]. 네이버·토스 커뮤니티는 링크아웃 전용(프레임 삽입·크롤링 금지) |
| LLM API | 토론, 요약, 구조화, 매도 판단 등 | `LlmPort` (Spring AI) | **목적별 다중 LLM [확정].** 지원 제공자: OpenAI, Claude, Ollama, DeepSeek. 코드에는 목적(`LlmPurpose`)만, 모델 매핑은 설정. 키는 클라이언트 Keychain. 상세 `docs/LLM_ROUTING.md` |
| 미러피시 | 다중 에이전트 시뮬레이션 | `SimulationPort` | 선택적, 관리형 설치(D13). 커밋 고정, 백엔드 API만 사용. 내부 API이므로 어댑터에 격리, 내장 프로세스/외부 주소 두 모드 |
| Slack | 알림 | `NotifierPort` | 봇 토큰, 사용자별 DM. 단독 모드에서는 클라이언트가 직접 발송 |

외부 API의 전체 목록(필수/권장/있으면 좋음), 엔드포인트, 호출 제한은 `docs/EXTERNAL_APIS.md`에 있다.

### 8.1 토스 API 확인 결과가 설계에 주는 영향 (2026-09-20 공식 문서 확인)

- **멱등성**: 동일 `clientOrderId`는 중복 접수되지 않는다. 자동 주문의 `clientOrderId`는 `(userId, 전략, 종목, 거래일, 회차)`에서 결정적으로 생성한다. lease, 주문 직전 조회와 함께 이중 주문 방어의 3중 장치다. [확정 사실]
- **허용 IP 사전 등록 필수**: 미등록 IP는 403. 접속 위치마다 IP가 바뀌는 "밖의 Mac"은 그대로는 토스 API를 쓸 수 없어 D5와 충돌한다. 대안 — (a) 고정 IP VPS를 토스 API 전용 송신 프록시로 사용(TLS 터널만 중계, 키·토큰 비노출), (b) 집 회선 VPN 경유, (c) 사용 장소 IP 수동 등록. **D18(같은 디바이스 공용)로 기본 환경에서는 집 회선 IP 하나만 등록하면 되어 문제가 크게 완화된다.** 집 회선 IP 변경 감지·알림을 둔다. 다른 디바이스 시나리오의 해법은 그때 결정 [보류]
- **클라이언트당 유효 토큰 1개**: 새로 발급하면 직전 토큰이 즉시 무효. 같은 `client_id`를 두 디바이스가 쓰면 서로를 무효화한다. 대안 — (a) 디바이스별 client 발급(가능 여부 확인 필요), (b) 토큰을 사용자 동기화 키로 암호화해 디바이스 간 공유, 발급은 lease 보유 디바이스만. **기본 환경(D18)에서는 사용자당 디바이스가 하나라 충돌이 없다.** 다른 디바이스를 쓸 때는 키도 동기화되므로(D17) (b)로 간다 [제안]
- **조건주문(SINGLE/OCO/OTO)은 토스 서버에서 동작**: Mac이 꺼져 있어도 손절·익절이 작동한다. 자동 매수 직후 OCO를 함께 거는 옵션을 F7에 둔다(종목당 OCO/OTO 1개 제약). [제안]
- **랭킹·종목 경고·시장 달력 API 제공**: 급등 탐지, 제외 필터, 세션·휴장일 계산을 공식 API로 구현한다.
- **WebSocket**: 계정당 동시 연결 2개, 연결당 구독 100건. 시세 채널은 유실 가능, `personal:order`는 재연결 시 재동기 필요. 멀티 디바이스에서는 lease 보유 디바이스가 실시간 연결을 우선 사용한다.
- **호출 제한**: 그룹별 초당 한도(ACCOUNT 1, ORDER 10, MARKET_DATA 15 등), 09:00~09:10 피크 제한. 어댑터에 그룹별 rate limiter 내장. 단시간 대량 주문은 어뷰징 제한 대상이므로 **분당 자동 주문 수 상한**을 가드레일에 추가한다. [제안]
- **과거 캔들 한계**(국내 2022-11-23, 미국 2021-11-30 이후, 1회 200개): 장기 백테스트는 KRX Open API·공공데이터·KIS로 보완한다.
- **모의투자 환경 없음**: "모의 실행" 단계와 fake `TradingPort`가 유일한 안전한 검증 수단이다.
- **멱등 키 제약 (2026-09-23 OpenAPI v1.2.17 확인)**: `clientOrderId`는 **최대 36자(영숫자·`-`·`_`), 유효기간 10분**. 10분이 지나면 같은 키도 새 주문으로 처리된다 → 결정적 키는 36자 안에서 만들고(SHA-256 base32 전체는 52자라 불가 → 26자 축약 또는 ULID), **재시도는 10분 안에서만 멱등을 믿고 그 밖에는 반드시 조회 후 결정**한다. 같은 키에 다른 본문은 422 `idempotency-key-conflict`. [확정 사실]
- **주문 형태 제약**: 호가 유형 `LIMIT`/`MARKET`, 유효 조건 `DAY`(기본)/`CLS`(미국 LOC, LIMIT만)/`OPG`(국내 시가단일가). **소수점 수량은 미국 시장가 매도에만**, 금액(`orderAmount`) 주문은 미국 시장가 매수에만, 둘 다 정규장 시작~종료 1시간 전에만 접수, 소수점 6자리. 정정은 국내 가격+수량, **미국은 가격만**. 1억원 이상은 `confirmHighValueOrder=true` 필수, 30억원 초과 불가. 같은 종목 반대 방향 미체결이 있으면 409. 호가 단위 불일치 시 400에 `tickSize`·`nearestPrices`가 온다. [확정 사실]
- **랭킹 제약**: `TOP_GAINERS`/`TOP_LOSERS`는 `realtime` 미지원(1d~1y만). 거래대금·거래량 랭킹만 실시간. `excludeInvestmentCaution` 필터, 토스증권 체결 기준/시장 기준 구분. → F11 "실시간 급등락"은 **1d 등락률 랭킹 상위 100을 받아 현재가 다건 조회(200개/호출)로 우리가 재정렬**한다. [확정 사실]
- **캔들은 1분봉·일봉만**(200개/페이지, `nextBefore` 페이지네이션, 수정주가 `adjusted`). 3·5·10·30·60분·주·월·년은 데몬이 집계한다. 1분봉의 `timestamp`는 봉 종료 시각. [확정 사실]
- **매수 유의사항에 VI(정적·동적) 발동이 수 초 내 반영**된다. 단기과열·정리매매·신주인수권도 같은 API. 미국은 상·하한가 없음(null). 국내 종목 정보에 NXT 지원·거래정지, ETF/ETN 레버리지 배수, 우선주 여부, 상장일이 온다. [확정 사실]
- **장 캘린더**: 국내는 KRX+NXT 통합 기준 프리(08:00~09:00)·정규(09:00~15:30, 종가단일가 15:20~)·애프터(15:30~20:00), 부분 휴장 표현 가능. 미국은 **데이마켓(토스 자체, KST 09:00~16:50)**·프리·정규·애프터 네 세션을 KST로 준다. [확정 사실]
- **계좌**: 종합매매(BROKERAGE)만, 자녀계좌 불가. 여러 계좌면 `accountSeq`로 선택. 매수 가능 금액은 통화별(KRW/USD) 현금 기준. 보유 조회에 수수료·세금과 원화 환산 손익률이 함께 온다. [확정 사실]
- **이용 조건**: 본인 매매 목적 한정, 상업적 이용·제3자 배포 금지. 가족 각자가 본인 키로 본인 계좌만 매매하는 D10과 부합한다.
- **정정·취소 규격 (2026-09-24 OpenAPI v1.2.17 확인)**: 정정은 `orders/{orderId}/modify`(국내 가격+수량 필수, 미국 가격만), 취소는 `orders/{orderId}/cancel`. **둘 다 새 `orderId`를 발급**하고 원주문은 `REPLACED`/`CANCELED`로 닫힌다. 정정·취소 거부는 별도 레코드(`REPLACE_REJECTED`/`CANCEL_REJECTED`)로 생기고 원주문은 이전 상태로 복귀. 이미 체결된 주문의 정정·취소는 409. 주문 목록은 `status=OPEN`(전량)/`CLOSED`(커서, 최대 100). 체결은 주문 1건 1행(건별 미제공). 실시간 `personal:order`는 세션 안 무손실, 재연결 시 `OPEN` 재동기 필요. → F14. [확정 사실]

## 9. 기능 명세

| # | 기능 | 단계 |
|---|---|---|
| F1 | 일반 매수·매도 | 2 |
| F2 | 기간별 손익 조회 | 2 |
| F3 | 차트 조회와 그래프 | 2 |
| F4 | 종목 조회와 선택 | 2 |
| F5 | AI 분석 기반 유력 종목 추천 | 4 |
| F6 | 매매 시 AI 인격 간 토론과 결과 안내 (미러피시 연동) | 4, 9 |
| F7 | 자동 매수 | 6 이후 |
| F8 | 자동 매도 | 6 이후 |
| F9 | 주식시장 전망 분석 리포트 | 4 |
| F10 | AI의 주식시장 학습 | 5 이후 지속 |
| F11 | 실시간 급등락 (당일 급등·급락 순위 서랍) | 2 |
| F12 | 종목 커뮤니티 (왼쪽 서랍: 링크아웃 + 미국 StockTwits 피드·자동 번역 + 국내 네이버 검색·RSS·수급 + 가족 메모) | 3 |
| F13 | 연기금종목 — 국민연금 해외투자 현황 (정보 모달 + 갱신 푸시) | 3 |
| F14 | 주문 관리 (체결 현황 · 미체결 정정·취소 · 주문 내역) [제안 2026-09-24] | 2 |

**F1.** 3번 영역의 **매수 / 매도 버튼을 누르면 주문 모달**이 뜬다 [확정]. 바깥 화면은 흐리게 하지 않고 모달 테두리에만 발광 효과(매수 빨강 계열, 매도 파랑 계열 — 제목 색 동일). 기본 위치는 앱 정중앙, 제목 바를 끌어 이동할 수 있고 위치는 앱 실행 중 유지(메모리), 재시작 시 초기화. 모달 안에 현재가가 실시간으로 표시된다. 주문 버튼은 두 가지: **지정가 매수/매도**(사용자가 넣은 가격·수량으로 확인 창을 거쳐 주문) · **현재가 즉시 매수/매도**(확인 창 없이 그 순간의 현재가로 즉시 주문 — 시장가가 아니라 **현재가 지정가**로 내보내 급등락 시 예상 밖 체결을 막는다 [확정]). 가드레일 판정은 모달 안에 실시간 표시되고, 미통과 시 두 버튼 모두 잠긴다. 즉시 주문도 가드레일과 step-up 인증(원격·고액)을 우회하지 못한다. 조건 주문(OCO/OTO)은 별도 탭. 토론(F6)은 조언이지 수동 주문의 차단 장치가 아니다.

**F2.** 체결 원본은 토스 → API 조회 후 로컬 캐시. 실현/평가 손익 구분, 수수료·세금 반영, 해외는 원화 환산과 현지 통화 병기. **모든 매수 lot에 출처 태깅**: `MANUAL / AI_RECOMMENDED / AUTO_BUY`. 이 태그는 손익 분석, F7 한도 계산, F10 학습의 공통 기반이다.

**F3.** 차트는 두 모드를 가지며 **"자세한 차트" 토글**로 전환한다 [확정].
- **간단 모드(기본, 토글 꺼짐)**: 현재가를 따라 실시간으로 그려지는 **선 차트** 하나. 구간 내 최고·최저를 점과 값으로 표시하고 현재가에 점선 기준선. 상승 구간 빨강·하락 파랑. 기간 탭: **장중(오늘) · 1일 · 1주 · 3달 · 1년 · 5년 · 전체**.
- **자세한 모드(토글 켜짐)**: **캔들(양봉 빨강·음봉 파랑) + 이동평균선 5/20/60/120(색 고정: 초록·빨강·주황·보라) + 거래량(막대 + 20일 평균)**. 구간 최고·최저 캔들에 값·현재가 대비 등락률·날짜를 표시하고, 오른쪽 축에 현재가 태그와 점선. 기간 탭: **분(1·3·5·10·30·60분 선택) · 일 · 주 · 월 · 년**. 보조지표는 설정에서 추가.
- **토글 상태는 사용자 설정으로 저장**한다. 종목을 바꿔도, 앱을 종료하고 다시 켜도 유지되며 디바이스 간 동기화된다. 기간 탭의 마지막 선택도 모드별로 기억한다.
- **실시간**: 두 모드 모두 체결 스트림으로 갱신된다. 간단 모드는 선의 끝점이, 자세한 모드는 마지막 봉과 거래량이 움직이고, 현재가·호가는 틱 단위 갱신(D21). 끊기면 "지연" 표시.
- 두 모드 모두 차트 위에 본인 매매 시점과 AI 토론·추천 시점을 마커로 표시한다.
- 프론트는 TradingView Lightweight Charts 1순위 검토. OHLCV 로컬 캐시, 이동평균·거래량 평균 등 지표 계산은 데몬.

**F4.** 종목 마스터 일 1회 동기화. 종목명·코드·초성 검색. 관심종목 그룹. 투자경고·위험·관리종목·거래정지 플래그(F7 제외 필터로도 사용).

**F5.** 2단 깔때기. 1단: 코드로 하는 정량 스크리닝(유동성, 추세, 거래량, 밸류에이션, 수급, 공시·뉴스) → 수십 종목. 2단: 상위 후보에만 RAG + LLM 분석·약식 토론(해외 종목은 F13 국민연금 보유·분기 변동을 근거 자료에 포함 [확정 2026-09-24]). 결과는 구조화 레코드(사유, 반대 논거, 확신도, 유효 기간)로 저장하고 F10에서 대조.

**F6.** 종목을 선택하면 먼저 **개요(업종·시총·PER/PBR·배당·최근 공시·종목 상태)와 AI 전망 요약**(LLM 1회, 캐시, 출처 포함, 확신도 없음)이 보인다. 토론은 사용자가 **테마를 골라 요청할 때** 시작한다. 테마는 **매수 토론 · 매도 토론 · 산업 동향**(업종 전반 분석 → 투자 의견) 세 가지. 빠른 토론(자체 페르소나 2~3라운드 + 사회자, 1분 안팎)과 심층 토론(미러피시, 백그라운드, 완료 시 알림) 모두 같은 `Verdict`(결론, 확신도, 핵심 근거, 반대 논거, 뒤집을 이벤트, 페르소나별 입장, 인용 문서)를 낸다. 진행 중인 토론은 **메신저 UI**로 실시간 표시되고(페르소나 말풍선, 입력 중 표시, 출처 번호), 사용자는 도중에 **질문·반박·동의·자료 추가·결론 요청**으로 개입할 수 있으며 페르소나는 반드시 응답한다. 토론은 `DebateSession`으로 저장되며(페르소나 정의 스냅샷, 라운드 기록, 결론 이력, 자료 스냅샷, 개입 기록) **같은 인격들로 재개**할 수 있다(정의가 바뀌었으면 새 정의). 재개 시 이전 결론 이후 새로 들어온 자료를 넣고 "무엇이 달라졌는가"부터 토론한다. 전망 요약은 주 1회 갱신. 산업 동향의 동종 종목은 같은 산업 분류 시총 상위 **10개** + 보유·관심 종목, 사용자 편집 가능, 상한 14 [확정]. 페르소나 정의는 사용자 편집 가능(버전 관리). 상세 `docs/DEBATE_DESIGN.md`.

**F9.** 장 시작 전 일간 + 주간 리포트. 입력: 지수·업종, 거시 지표, 뉴스·공시, 보유·관심 종목 이슈. Slack에는 요약과 링크만.

**F10.** LLM 재학습이 아니다. 네 가지의 조합이다.
- 성과 추적: 모든 추천·토론 결론·리포트 전망을 1일·5일·20일 뒤 실제 수익률과 자동 대조.
- 페르소나 보정: 페르소나별·국면별 적중률 → 종합 판단 가중치.
- 회고 메모리: 크게 틀린/맞은 사례의 사후 분석을 RAG에 저장, 유사 상황 토론에 주입.
- 스크리닝 튜닝: F5 1단 조건을 백테스트로 재조정.
- 가중치 변경은 최소 표본 수 이상에서만 적용, 변경 이력 보존, 페르소나 성적표 화면 제공.
- 가족 공유 [확정]: 종목·시장에 대한 기록만. 개인 매매 내역·수량·금액·손익은 제외. 단독 모드에서는 공유 없음.

**F11. 실시간 급등락.** 국내·미국 각각 **급등 5 · 급락 5**를 옛 포털 실시간 검색어처럼 순위로 보여주는 **오른쪽 슬라이드 서랍** [확정]. 화면 표기는 "실시간 급등락", 가장자리 탭은 "급등락" [확정]. 기준 탭은 등락률(기본) · 거래량 급증 · 체결 강도. **10초 간격 갱신** [제안]이고 순위 옆에 직전 갱신 대비 변동(↑n · ↓n · NEW)을 표시한다. 각 행에 현재가·등락률, 거래량 배수, 뉴스·공시 건수, 보유·토론 중·투자경고·관리·저유동성 칩을 함께 보여 F7 제외 필터의 근거가 그대로 보이게 한다. 미국은 정규장 밖에서는 프리·애프터마켓 또는 전일 기준으로 표시하고 그 사실을 칩으로 밝힌다. 데이터는 토스 랭킹 API [확인 필요: 등락률·거래량 순 랭킹의 제공 범위와 갱신 주기]와 F7(b) 급등 탐지기의 신호를 공유한다. 랭킹은 정보 표시일 뿐 자동 매수 트리거가 아니다 — 트리거는 F7(b)의 탐지기가 별도 규칙으로 판단한다.

**F12. 종목 커뮤니티.** **왼쪽에서 슬라이드로 나오는 서랍**이다(④ 토론 영역의 탭이 아니다) [확정]. 상단 바 💬 버튼 또는 ⌘U로 열고, 관심종목·급등락 목록의 각 종목 행에 있는 💬 버튼으로 **그 종목의** 커뮤니티를 연다. 서랍 겹침 규칙은 11.3. 토스증권 Open API에는 커뮤니티 엔드포인트가 없고(2026-09-23 전체 엔드포인트 확인), 네이버 종목토론실은 프레임 삽입·크롤링 모두 약관에 걸리므로 **공식 API가 있는 소스만 피드로, 나머지는 링크아웃**으로 한다.
- **링크아웃(국내·미국 공통)**: 네이버 종목토론실, 토스 앱 해당 종목, (미국) Reddit 검색·Yahoo Finance 토론을 외부 브라우저로 여는 버튼. 종목 코드로 URL을 만든다. 우리 창 안에 남의 페이지를 띄우지 않는다(`contextIsolation` 유지, 헤더 우회 금지).
- **Reddit은 링크아웃 전용** [확정 2026-09-23]: Reddit Data API는 Responsible Builder Policy에 따라 개인·비상업 프로젝트도 **사전 승인 필수**(예외 없음)이고, 승인되더라도 원작자가 지운 글의 삭제 의무와 48시간 내 저장 데이터 정리 권고, AI/ML 이용 금지가 붙어 "원문을 코퍼스에 보관해 `asOf`로 재검색"하는 우리 구조와 맞지 않는다. 승인을 받으면 **표시 전용(원문 미저장, 캐시 48시간 이내, RAG 미투입)** 어댑터를 `CommunityPort`에 추가할 수 있다 [보류].
- **미국 종목 피드 = StockTwits** [확정 2026-09-23]: 공개 비인증 엔드포인트로 종목별 Bullish/Bearish 심리 비율·메시지 스트림·트렌딩 종목을 가져와 피드 탭에 보인다(`CommunityPort` 구현체). 원문은 RAG 코퍼스에 `publishedAt`과 함께 넣어 토론 페르소나의 "여론·심리" 근거로 쓴다(약관상 저장 가능 범위는 착수 전 확인 [확인 필요]). 포트는 `NewsPort`와 같은 구조. 개인 비상업 용도로만 쓰고 엔터프라이즈 API는 쓰지 않는다. IP당 시간당 200회 한도이므로 **커뮤니티 서랍을 연 종목만 조회하고 캐시 TTL 10분, 백그라운드 폴링 없음**. **MCP 서버는 제품에 넣지 않는다** — StockTwits 공식 MCP 서버(MIT, Node)는 같은 공개 엔드포인트의 얇은 래퍼라 접근 권한·한도가 달라지지 않고, Node 런타임과 자식 프로세스 관리만 늘며, 토론 중 실시간 도구 호출은 기준 시점 `asOf` 원칙(RAG_DESIGN)과 충돌한다. MCP 서버는 개발 중 데이터 품질 검증(사전 실험)에만 쓴다. 상세 `docs/EXTERNAL_APIS.md` 3장.
- **자동 번역(영→한)**: `LlmPurpose.TRANSLATE`로 처리한다(기본 로컬 Ollama, 폴백 저가 제공자 — `docs/LLM_ROUTING.md`). 규칙: 목록은 **제목만 배치 번역**(한 요청에 20~30건), 본문·댓글은 **펼칠 때 번역**; 번역 결과는 `(post_id, 모델 식별자)` 키로 캐시; 번역문 옆에 **원문 보기 토글**; RAG에는 원문만 저장; **투자 은어 용어집**(DD·tendies·bagholder 등)을 시스템 프롬프트에 넣고 사용자가 편집 가능; 번역 실패 시 원문 그대로 표시하고 "번역 안 됨" 표시. 전용 번역 API(DeepL·Google·Papago)는 품질·속도가 아쉬울 때 `TranslationPort`로 추가 [제안].
- **국내 종목 탭** [확정 2026-09-23]: 원문을 합법적으로 가져올 국내 커뮤니티 소스는 없으므로 "커뮤니티 피드"는 두지 않고, 성격이 다른 세 데이터와 네이버 검색으로 채운다.
  - **뉴스 — RSS가 1차 소스.** 언론사 공식 RSS(경제지·통신사)와 Google News RSS의 한국어 종목명 검색 피드에서 제목·링크·발행 시각을 받는다(발행 시각이 정확해 기준 시점 원칙에 유리). 본문은 각 사이트 robots.txt·약관이 허용하는 범위에서만 색인. 빅카인즈 뉴스 API는 비상업 이용 조건 확인 후 후보 [확인 필요].
  - **네이버 검색 API(뉴스·블로그·카페)를 추가 소스로 쓴다** [사용자 결정]. 이미 발급된 키, 개인 단독 사용 전제. 제목·요약·링크·`pubDate`를 RSS 목록과 합쳐 보이고(중복은 URL 기준 제거), 블로그·카페 결과는 국내 개인 투자자의 글을 잡는 보조 채널이다. 약관(2026-09-07, AI 입력·저장 금지)과 API HUB 이관·유예 종료(2027-06-30)의 위험은 `docs/EXTERNAL_APIS.md` 2.7에 기록한다. 어댑터는 설정만으로 뺄 수 있게 하고, 네이버 출처 문서는 코퍼스에서 일괄 삭제할 수 있게 `source=naver`로 태그한다.
  - **"여론"은 수급 수치로 대신한다.** 토스 API의 종목별 투자자별 매매(개인·외국인·기관), 신용 잔고, 공매도, 프로그램 매매, 거래량 급증 랭킹을 수급 요약 카드로 보인다. 게시판 글보다 노이즈가 적은 심리 지표이며 2번 영역 수급 탭과 같은 데이터다.
  - **공시는 DART.** 국내 "재료" 근거의 1순위.
  - 국내 탭 구성: **링크아웃(네이버 종목토론실·토스 앱) + 뉴스 목록(RSS + 네이버 검색) + 수급 요약 + 가족 메모**. 국내 **여론 요약(LLM digest)은 하지 않는다** — 근거가 될 커뮤니티 원문이 없다.
- **미국 종목 탭**: StockTwits 피드·번역·여론 요약 + 링크아웃 + 가족 메모.
- **여론 요약**: 미국 종목에 한해 StockTwits 원문을 `DOCUMENT_DIGEST`로 요약해 "오늘 이 종목 여론" 한 단락을 탭 상단에 보여준다.
- **우리 안의 커뮤니티**: 가족 구성원이 종목별로 남기는 매매 일지·메모(가족 공유 키로 동기화, 개인 매매 수량·금액 제외)를 같은 탭에 보인다 [제안].
- **토론 자료로의 투입** [확정 2026-09-23]: F12가 모으는 여론·수급 데이터는 **빠른 토론(자체)과 심층 토론(미러피시) 모두의 기본 자료**다. 텍스트(StockTwits 메시지, 네이버 블로그·카페·뉴스, RSS)는 RAG 공용 코퍼스에 `COMMUNITY`/`NEWS`로 들어가 `[C]`/`[N]` 등급으로 인용되고, 수치(심리 비율·메시지 수·투자자별 순매수·신용·공매도·거래량 배수)는 일별 스냅샷으로 저장해 "여론·수급 요약" 표로 프롬프트와 미러피시 시드 문서에 들어간다. 여론은 심리 지표이지 사실 근거가 아니며(사회자가 등급을 강제), 페르소나별 가중이 다르고, 강세·약세 균형으로 최대 10건(빠른)/20건(미러피시)만 투입한다. 상세 `docs/DEBATE_DESIGN.md` 3.2, `docs/RAG_DESIGN.md` 4.3.
- 커뮤니티 내용은 정보 표시와 토론 근거일 뿐, 어떤 자동 주문의 트리거도 아니다.

**F13. 연기금종목 — 국민연금 해외투자 현황** [확정 2026-09-24]. 국민연금의 해외주식 종목별 보유를 **두 출처**로 내려받아 저장한다: **SEC EDGAR 13F-HR**(분기, 미국 상장분·ADR, 주식 수·평가액 USD, 분기 말 후 45일 안 제출, CIK 0001608046)과 **공공데이터포털 국민연금공단 해외주식 투자정보**(연 1회, 전 지역, 연말 기준 평가액·비중·지분율, 10억원 미만 제외). 종목 단위 실시간·일간·주간 공개는 어디에도 없어 분기가 가장 촘촘한 주기다. 이를 데몬이 내려받아 저장하고, 상단 바 **🏛️ 연기금종목** 버튼(☰ 관심종목과 ⚡ 실시간 급등락 사이, ⌘N)으로 여는 **정보 모달**에서 보여 준다. 버튼 이름은 "연기금종목"이지만 자료는 국민연금 단독이므로 모달 제목은 "국민연금 해외주식 투자 현황"이다. 모달은 주문 모달과 같은 틀(바깥 흐림 없음, 강조색 발광, 제목 바 드래그)이며 요약 4칸 → 탭 넷(분기 13F · 연간 전체 · 전년 대비 변화 · 내 종목) → 표(13F: 주식 수·평가액 USD·전분기 대비 / 연간: 평가액·비중·지분율·전년 대비) 순서다. 토론 개요 탭에 "국민연금 보유" 행(해외 종목만)을 둔다. **갱신 감지**는 데몬이 앱 기동 시(마지막 확인 24시간 경과)와 매일 06:30 KST에 EDGAR 제출 목록(새 접수번호·정정)과 Swagger 카탈로그(새 연도 데이터셋)·내용 해시를 확인해서 하고, 변화가 있으면 버튼 배지 · 토스트 · 알림 센터 · (켠 사용자) Slack DM · (창이 닫혀 있으면) macOS 알림으로 **푸시**한다. 같은 변화는 한 번만 알리고 확인은 사용자별. **알림 기본값은 앱 안 켬 · Slack 끔.** **캐시는 cache-aside, TTL 24시간**: 화면은 데몬의 SQLite 스냅샷만 읽고, 데몬은 마지막 성공 확인이 24시간을 넘겼을 때만 출처를 다시 부른다("지금 확인"만 예외, 1분 간격). **종목 매핑**: 13F는 CUSIP(토스 종목 마스터의 ISIN 3~11자리), 연간은 회사명(종목 마스터 영문명 정확 일치 + 사용자 편집 별칭 표), 미매칭은 칩 없이 표시하고 LLM 추정은 쓰지 않는다. 토론의 심리·수급 스냅샷 표에 "국민연금 보유" 줄을 넣는다(`docs/DEBATE_DESIGN.md` 3.2). 키는 공공데이터포털 서비스 키(공유 키, admin, 선택). EDGAR는 키 없이 쓰되 User-Agent에 넣을 연락처 이메일을 admin 공유 설정으로 둔다. 정보 표시와 토론 근거일 뿐 **어떤 자동 주문의 트리거도 아니다**. **AI 투자 판단의 근거 자료 중 하나다 [확정 2026-09-24]**: 토론 개요의 "국민연금 보유" 행, 빠른 토론의 수치 스냅샷 블록, 미러피시 시드 문서의 여론·수급 절, F5 2단 분석 입력에 들어가며 `[S]` 등급(공공기관 사실 자료, 기준일 명시)으로 인용되고 `Verdict`의 근거에 기록된다. 상세 `docs/NPS_HOLDINGS_DESIGN.md`.

**F14. 주문 관리** [확정 2026-09-24]. 주문을 낸 뒤의 체결 현황과 미체결 정정·취소. 새 화면을 두지 않고 **3번 영역 오른쪽 표를 보유 · 미체결 · 오늘 체결 세 탭**으로 나누며, 미체결 행에 정정·취소 버튼을 둔다. 정정은 주문 모달과 같은 틀의 **정정 모달**(원주문 카드 · 실시간 현재가 · 새 지정가/수량 · 가드레일)에서 하고, **지정가만**(토스 규격은 시장가 정정도 허용하지만 F1 원칙대로 막음), **국내는 가격·수량, 미국은 가격만**(토스 규격), **항상 확인 창**. 버튼은 셋: 정정 주문(입력값) · **현재가로 정정**(그 순간의 현재가 지정가로 가격만, 수량 그대로 — 즉시 주문과 같은 원칙) · 주문 취소. 취소는 확인 창 한 번. 정정 가능 수량은 **잔량까지**(100주 중 24주 체결이면 최대 76주). **자동 주문(`AUTO_BUY`·`AUTO_SELL`) 미체결도 사람이 정정할 수 있다**: 사람의 정정은 트리거가 사람이므로 수동 주문으로 취급해 자동 주문 한도 규칙의 대상이 아니지만, lot 출처는 자동으로 남겨 노출액에 계속 계산한다. 정정 결과가 자동 매수 한도를 넘으면 **확인 창을 한 번 더** 거친다(10.1 참조, 자동매매 한도의 유일한 예외). 정정 주문의 `clientOrderId`는 **새로 발급**(ULID)하고 원주문과 체인으로 잇는다. 상태는 토스 실시간 주문 채널로 갱신되고 체결 시 토스트·알림 센터(Slack은 설정). 정정·취소는 토스가 새 주문번호를 발급하므로 로컬 주문 기록은 원주문→새 주문 체인을 갖고, 정정 처리 중 둘이 함께 열려 있는 동안 "같은 종목·방향 미체결" 검사는 체인을 한 건으로 본다. 결과를 모르는 요청(타임아웃)은 조회로 확정하기 전 재시도하지 않는다. 전체 기간은 "주문 내역" 화면(토스 종료 주문 조회 커서를 로컬 캐시에 적재, F2 손익의 원천). 상세 `docs/ORDER_MANAGEMENT_DESIGN.md`.

## 10. 자동화 규칙 (F7, F8) — 코드로 강제

### 10.1 공통

- 실행 단계 [확정]: **모의 실행**(주문 없이 기록, 가상 손익 추적) → **승인 후 실행**(Slack 알림 → 웹/클라이언트에서 step-up 승인, 승인 요청에 만료 시간) → **완전 자동**(한도 안에서 즉시 주문, 사후 알림).
- 단계 설정 단위: 사용자별 × 시장별(국내/해외) × 매수/매도. 매도는 종목별로도.
- 단계 상향 화면에 모의 실행 성적(건수, 승률, 가상 손익, 최대 손실, 종목 집중도) 표시.
- **킬 스위치**: 클라이언트·웹 어디서든 한 번에 해당 사용자의 모든 자동화 정지. 연속 손실·일일 손실 한도 도달 시 자동 발동.
- 자동화는 lease를 가진 디바이스에서만 실행.
- Slack 야간 방해 금지 설정(긴급 알림만 통과).
- 한도 상수는 설정으로 **낮출 수만** 있고 코드 상수 이상으로 올릴 수 없다.
- **유일한 예외 [확정 2026-09-24]**: 사람이 자동 주문의 미체결을 **직접 정정**할 때(F14). 사람의 정정은 수동 주문으로 취급되어 한도 규칙의 대상이 아니지만, 정정 결과가 자동 매수 한도를 넘으면 경고 규칙이 **확인 창을 한 번 더** 띄우고 사람이 승인해야 진행된다. lot 출처는 자동으로 남아 노출액에 계속 계산되므로 초과 상태에서는 새 자동 매수가 막힌다. 승인은 감사 로그에 남는다. 우회 플래그·설정이 아니라 건별 명시적 승인이다.

### 10.2 자동 매수 (F7)

| 규칙 | 값 | 상태 |
|---|---|---|
| 총 노출액 한도 | **1000만원, 국내·해외 각각** | 확정 |
| 노출액 정의 | 출처 `AUTO_BUY`이고 매수 후 **7일(168시간) 미경과**인 미청산 lot의 매입 원가 합 + 미체결 자동 매수 주문 금액 | 확정 |
| 한도 복원 | 매도 청산 시, 또는 보유 7일 초과 시(일반 보유로 편입, Slack 알림 + 자동 매도 허용 여부 안내) | 확정 |
| 해외 노출액 환산 | 매수 시점 환율의 원화 매입 원가 | 확정 |
| 종목당 상한 | 없음 (설정 항목은 만들되 기본값 "없음") | 확정 |
| 하루 신규 매수 상한 | 1000만원 (시장별) | 확정 |
| 동시 보유 자동 매수 종목 | 최대 8개, 국내·해외 각각 | 확정 |
| **예수금 하한(2차 제한)** | 자동 매수 설정 시 사용자가 하한 금액을 지정. 하한은 **설정 시점 예수금의 50% 이상**이어야 함. 주문 후 예수금이 하한 아래로 내려가는 자동 매수는 거부 | 확정 |
| 예수금 하한의 시장별 적용 | 국내(원화 예수금)·해외(외화 예수금) 각각 설정 | 제안 |
| 제외 필터 | 투자경고·위험·관리종목, 거래정지, 상한가 근접, 저유동성, 신규 상장 직후, 사용자 지정 제외 | 확정 |
| 급등 매수 진입 조건 | 당일 상승률 상한(이미 많이 오른 종목은 추격하지 않음) | 확정 |
| 중복 방지 | 주문 직전 토스 API로 미체결·당일 체결 확인 | 확정 |

**트리거 두 가지.** (a) F5가 식별한 유력 종목 → 빠른 토론을 거쳐 분할 매수. (b) 당일 급등 조기 포착 → **규칙 기반 탐지기**(거래량 급증, 체결 강도, 호가 변화, 뉴스·공시)가 포착하고 LLM은 "실체 있는 재료가 있는가"만 빠르게 확인. 급등 추격은 손실 위험이 가장 큰 기능이므로 모의 실행 단계를 길게 가져간다.

**거래 시간.**
- 국내: 정규장만. 장 시작 직후와 마감 직전 동시호가 구간 제외. [확정]
- 미국: 한국 시간 **22:30~07:00**, 애프터마켓 포함. [확정]
- 구현은 고정 KST가 아니라 `America/New_York` 시간대와 거래소 휴장일 달력으로 세션(프리/정규/애프터)을 계산하고, 위 KST 창과 교집합을 취한다(서머타임 자동 반영). [제안]
- 미국 정규장 개장 직후·마감 직전 N분 제외, 시간외 세션에서는 지정가 주문만. [제안]

### 10.3 자동 매도 (F8)

- 대상: 사용자가 **종목별로 자동 매도를 허용**해 둔 보유 종목. AI가 당일 매도가 이득이라 판단하면 매도. 판단은 수수료·세금 반영 순손익 기준. [확정]
- 한도: 종목별 **기준 수량의 90%**. 기준 수량 = 허용 설정 시점의 보유 수량(추가 매수 시 갱신, 사용자가 허용을 재확인하면 재설정). 누적 자동 매도 수량이 기준의 90%를 넘을 수 없다. 나머지 10%는 사람만 매도. [확정]
- 손절 포함 여부: 종목별 허용 설정에서 "수익 실현만 / 손절 포함" 선택. [확정]
- 자동 매수된 lot은 자동으로 매도 허용 대상이 되지 **않는다**. "자동 매수분은 매수 즉시 자동 매도 허용" 옵션 제공(기본 꺼짐). [제안]
- 분할 매도 기본.

### 10.4 LLM과 주문의 경계

LLM 출력은 주문 파라미터로 **직접 쓰지 않는다.** 반드시 구조화 → 스키마 검증 → 가드레일 검사 → (단계에 따라) 승인을 거친다. 가드레일 검사는 LLM 바깥의 순수 코드이며 `core` 패키지에 둔다.

## 11. 리포 구조

### 11.1 구조 [확정: Java는 단일 Spring Boot 프로젝트]

```
stockholm/
├─ backend/                      # 단일 Spring Boot 프로젝트 (Gradle, Kotlin DSL)
│  └─ src/main/java/banghak/stockholm/   # 루트 패키지 banghak.stockholm
│     ├─ core/                   # 순수 도메인 — 프레임워크 import 금지
│     │  ├─ money/  trading/  automation/  guardrail/  eventlog/  lease/
│     │  └─ port/                # 모든 외부 세계의 인터페이스 (TradingPort, LlmPort, Clock …)
│     ├─ engine/                 # @Profile("engine") — 클라이언트 데몬
│     │  ├─ broker/              # 토스 어댑터(주문·시세·계좌), 타 증권사 정보 어댑터
│     │  ├─ asset/               # 금융결제원 본인인증·자산 조회
│     │  ├─ research/            # DART, 뉴스 수집, Lucene RAG
│     │  ├─ debate/              # 페르소나 토론, 미러피시 어댑터
│     │  ├─ recommend/  report/  learning/
│     │  ├─ automation/          # 자동 매수·매도 실행기, 스케줄러
│     │  ├─ sync/                # 동기화 에이전트, lease 클라이언트
│     │  ├─ notify/              # Slack 직접 발송
│     │  ├─ secret/              # Keychain 접근
│     │  └─ localapi/            # 127.0.0.1 REST·WebSocket, 로컬 토큰
│     ├─ relay/                  # @Profile("relay") — optional 서버
│     │  ├─ member/  auth/  device/
│     │  ├─ routing/             # Command/Event 중계
│     │  ├─ mailbox/             # 암호문 store-and-forward
│     │  ├─ lease/  slack/
│     │  └─ web/                 # 원격 웹 UI 정적 서빙
│     ├─ shared/                 # 프로토콜 타입(생성물), 암호 유틸, 공통 설정
│     └─ StockholmApplication.java
├─ desktop/                      # Electron + React + TypeScript (로컬/원격 겸용 UI, 웹 타깃 빌드 포함)
├─ protocol/                     # JSON Schema — backend와 desktop이 각각 타입 생성
├─ docs/
├─ CLAUDE.md
└─ PROJECT.md
```

**클라이언트 우선 개발 시 손대는 곳:** `backend/…/core` → `backend/…/engine` → `desktop/`. `relay/`는 7단계 전까지 열지 않는다.

**경계 규칙 (멀티모듈 대신 테스트로 강제).**
- `core`는 Spring, JPA, Jackson, HTTP 클라이언트 등 어떤 프레임워크도 import하지 않는다.
- `engine`과 `relay`는 서로를 참조하지 않는다. 둘 다 `core`와 `shared`만 참조한다. (`relay`는 `core` 중 lease·eventlog 봉투 정도만 쓴다.)
- `engine` 하위 패키지끼리는 공개 API(패키지 최상위 타입)로만 협력한다.
- 위 규칙은 **Spring Modulith의 모듈 검증 테스트와 ArchUnit 테스트**로 빌드에서 강제한다.
- 프로필별 컨텍스트 테스트: `engine`만, `relay`만, 둘 다 켠 세 경우 모두 기동되는지, 그리고 **`relay`만 켰을 때 주문·증권사·LLM 관련 빈이 하나도 로드되지 않는지** 검증한다.

**실행 형태.** 같은 산출물을 프로필로 구분한다: 클라이언트 `engine` / 서버 단독 `relay` / Mac mini 겸용 `engine,relay`.

기술 스택: **OpenJDK 21 (LTS)**, 루트 패키지 `banghak.stockholm`, Spring Boot 최신 안정판, Spring Modulith, Gradle(Kotlin DSL), SQLite, Flyway, Lucene, Spring AI / Electron, React, TypeScript, electron-builder. 대상은 Apple Silicon(arm64) 우선.

### 11.2 빌드·패키징과 GraalVM 네이티브 이미지 방침 [제안]

**기본 경로는 JVM이다.** Gradle `bootJar` → `jlink` 경량 JRE → electron-builder `extraResources`로 `.app`에 포함 → dmg. 최상위 명령 하나로 전 과정. 코드 서명·공증은 가족 Mac 배포 시점에 결정.

네이티브 이미지에 대한 판단은 다음과 같다.

| 관점 | 평가 |
|---|---|
| 메모리·기동 | 상주 100~150MB, 기동 0.1초대. 다만 24시간 상주 데몬에 32GB 환경이라 실익이 작다 |
| 배포 | JRE 번들이 사라져 dmg가 작아지고 서명·공증이 단순해짐(단일 바이너리, JIT entitlement 불필요) |
| 처리량 | JIT가 없어 지표 계산·백테스트 같은 CPU 작업의 최대 성능은 JVM보다 낮다 |
| **프로필과의 충돌** | Spring AOT는 **빌드 시점에 프로필과 조건부 빈을 확정**한다. 실행 시 `engine`/`relay`를 고르는 방식이 네이티브에서는 동작하지 않으므로 조합별로 바이너리를 따로 빌드해야 한다 |
| 라이브러리 위험 | Hibernate, SQLite JDBC(JNI), Lucene(메모리 맵·MethodHandle), Spring AI, Keychain 접근에서 각각 힌트·호환성 확인이 필요. 하나라도 막히면 우회 비용이 크다 |
| 개발 경험 | 빌드 수 분, 교차 컴파일 불가(macOS용은 Mac에서, VPS용은 Linux에서), JVM과 네이티브의 동작 차이를 잡을 별도 테스트 필요, 디버깅·프로파일링 도구 제약 |

방침: **클라이언트 데몬은 JVM으로 개발·배포한다.** 네이티브 이미지는 의존성이 적고 저사양 VPS에서 효과가 큰 **`relay` 단독 서버 빌드에 한해** 7단계에서 검토한다. 나중의 전환 가능성을 닫지 않기 위해 과도한 리플렉션·동적 프록시·런타임 클래스 생성에 의존하는 코드와 라이브러리를 피하고, 새 의존성 추가 시 네이티브 호환 여부를 확인해 둔다.

## 11.3 화면 구성 [확정]

메인 화면은 네 영역이 고정된 한 화면이다(스케치 기반). 상단 바와 하단 상태줄(데몬·API·LLM 예산·Ollama)이 감싼다. **상단 바의 순서 [확정, 2026-09-24 개정]**: Stockholm(브랜드) · 종목명(클릭 → 검색 팝오버) · 💬 커뮤니티 · ☰ 관심종목 · **🏛️ 연기금종목**(F13 모달, ⌘N, 새 데이터 배지) · ⚡ 실시간 급등락 · (오른쪽 정렬) 장 상태 KR/US · 자동화 단계 · 사용자 · **전체 정지**.

| 영역 | 위치 | 기본 비율 | 내용 |
|---|---|---|---|
| 1 차트 | 좌상 | 왼쪽 열 폭 60%, 그 안에서 높이 70% | "자세한 차트" 토글(설정 저장). 꺼짐: 실시간 선 차트 + 최고·최저. 켜짐: 캔들·MA 5/20/60/120·거래량. 매매·토론 마커, 모드별 기간 탭 |
| 3 매수·매도 | 좌하 | 왼쪽 열 높이 30% | 현재가·가능 금액·보유 요약 + **매수/매도 버튼(→ 주문 모달)** + 보유·손익 요약(출처 칩, 자동 매수 노출/한도). 오른쪽 표는 **보유 · 미체결 · 오늘 체결** 세 탭, 미체결 행에서 정정·취소(F14) |
| 2 가격 | 우상 | 오른쪽 열 폭 40%, 그 안에서 높이 40% | 현재가, 호가, 요약 수치, 종목 상태 칩 |
| 4 토론 | 우하 | 오른쪽 열 높이 60% | 개요·전망·동종 종목 → 테마 선택(매수/매도/산업 동향) → 진행 중(메신저 UI, 사용자 개입 입력창) → 기록·재개 |

**테마 [제안 2026-09-24]**: 기본은 **운영체제 테마 자동**(macOS 라이트/다크 전환 시 즉시 반영, Electron `nativeTheme`). 설정에서 **시스템 / 라이트 / 다크**를 고를 수 있고 사용자 설정으로 저장·동기화한다. 두 팔레트는 화면 설계서(`docs/screens/MAIN_SCREEN_DESIGN.html`)의 색 토큰이 기준이며, 상승 빨강·하락 파랑은 다크에서 밝기를 올린 값(`#ff5a66`·`#5c9cff`)을 쓴다. 모든 화면은 토큰만 쓰고 고정 색을 쓰지 않는다(차트 라이브러리 색도 토큰에서 주입). 원격 웹도 같다. Figma는 색 변수 컬렉션 "Stockholm 색"(라이트·다크 두 모드)으로 같은 팔레트를 둔다.

세 구분선(좌우, 왼쪽 열 상하, 오른쪽 열 상하)은 마우스로 조절하고 더블클릭으로 기본값 복원. 두 열의 가로 구분선은 독립적이다. 최소 25%. 비율은 사용자 설정으로 저장·동기화한다. **구분선은 보이는 선 1px, 마우스가 잡히는 영역은 양쪽 4px씩**(올리면 강조색, 끄는 동안 비율 배지) [확정]. **종목 검색은 상단 바의 종목명을 눌러 여는 팝오버**(이름·코드·초성, ⌘K) [확정].

**서랍 세 개** [확정]. 모두 네 영역을 덮지 않고 **밀어낸다**(네 영역 크기는 유지되고 반대쪽 끝이 창 밖으로 나간다). 가장자리 탭은 두지 않는다. **상단 바 버튼과 단축키로만** 열고 ✕·같은 버튼·Esc로 닫는다. 열린 서랍의 버튼은 강조색으로 표시된다. 평소에는 모두 닫혀 있다. 폭은 구분선으로 15~50% 조절, 더블클릭 복원, 설정 저장.

| 서랍 | 방향 | 기본 폭 | 단축키 | 종목 선택 시 |
|---|---|---|---|---|
| 관심종목 (F4) | 왼쪽 | 30% | ⌘L | 네 영역이 바뀌고 서랍은 닫힌다 |
| 커뮤니티 (F12) | 왼쪽, 관심종목보다 바깥 | 28% | ⌘U | 종목 행의 💬로 연다. 네 영역은 그대로(그 종목의 커뮤니티만 표시) [제안] |
| 실시간 급등락 (F11) | 오른쪽 | 25% | ⌘J | 네 영역이 바뀌고 서랍은 열린 채 유지 |

겹침 규칙: **왼쪽 두 서랍은 겹쳐 열린다.** 관심종목을 펼친 채 어느 종목의 💬를 누르면 화면은 |커뮤니티|관심종목|Main| 순으로 펼쳐진다. **오른쪽 급등락은 왼쪽 서랍과 동시에 열리지 않는다**(급등락에서 💬를 누르면 급등락이 닫히고 커뮤니티가 왼쪽에 열린다; 왼쪽 서랍이 열려 있을 때 급등락을 열면 왼쪽이 닫힌다). 관심종목 서랍은 그룹 탭, 드래그 정렬, 보유·토론 중 표시, 행마다 💬 버튼 [확정]. 주문 버튼은 3번 영역에만(토론 결과에 주문 버튼 없음). 상승 빨강·하락 파랑, 금액은 tabular 숫자 오른쪽 정렬. 원격 모드도 같은 화면에 step-up 인증만 추가. 나머지 화면(최초 구동 마법사, 손익, 추천, 자동화, 리포트, 학습 현황, 설정, 알림·승인 센터)과 목업은 화면 설계 아티팩트("Stockholm 화면 설계")와 Figma 기획안(https://www.figma.com/design/sZLAmrVRCfMgkfF7sS1uCx/Stockholm) 참조.

## 12. 개발 단계

1. **리포 골격**: `backend/`(단일 Spring Boot) + `desktop/`(Electron) 빌드 파이프라인, 경계 검증 테스트(Modulith·ArchUnit), 데몬 기동·로컬 토큰 접속, 로컬 프로필, dmg 산출, 메모리 실측
2. 토스 API 연동(시세·계좌·승인 기반 소액 주문), F1~F4, 금융결제원 본인인증·자산 조회
3. 데이터 수집과 RAG: DART, 뉴스, Lucene 인덱스
4. 페르소나 토론(세 테마, 개요·전망, DebateSession 저장·재개), 추천, 시장 리포트 (주문 미연결). 미러피시 사전 실험 결과 반영
5. 기록·대조: 성과 추적과 학습 기반
6. 가드레일, 자동화 모의 실행 → 승인 기반 주문, Slack 알림(클라이언트 직접 발송)
7. relay 서버: 회원·인증·디바이스, 원격 컨트롤, 서버 경유 Slack
8. 멀티 클라이언트 동기화와 lease, 가족 공유
9. 미러피시 어댑터, 완전 자동 단계 개방 검토

## 13. 확인이 필요한 사항

- 토스 OpenAPI: **밖의 Mac의 IP 허용 문제 해법(8.1)**, **멀티 디바이스 토큰 공유 방식(8.1)**, 디바이스별 client 발급 가능 여부. (소수점·프리마켓·랭킹·캔들 제약은 8.1에서 확인됨)
- **결정 필요**: 미국 주식의 소수점 보유분 매도(시장가 매도만 가능 — F1 "지정가만" 원칙의 예외를 둘지), 금액(`orderAmount`) 매수 지원 여부, 국내 `OPG`(시가단일가)·미국 `CLS`(LOC) 유효 조건을 주문 모달에 노출할지
- 금융결제원 키: 테스트베드/운영 여부, 자산 조회 범위
- 미러피시: 사전 실험으로 품질·비용·API 형태 확인, Zep Cloud 사용 여부·무료 한도, **관리형 설치 전제 조건**(프론트엔드 없이 백엔드 API만으로 전 과정 가능한지, 포트·데이터 경로를 환경 변수로 바꿀 수 있는지, `uv`만으로 시스템 Python 없이 설치되는지)
- 타 증권사 API: 실제로 쓸 증권사와 필요한 데이터 항목
- 10장의 [제안] 항목들에 대한 확정
- 토론: 미러피시의 중간 발언 실시간 노출 여부, 시드 문서 "여론·수급" 절의 실제 활용 여부 (`docs/DEBATE_DESIGN.md` 9장)
- F13 연기금종목: 13F 정보표 XML 파일명, 토스 미국 종목 마스터의 ISIN·영문 회사명 제공 여부, 공공데이터포털 일일 트래픽 한도 수치 (`docs/NPS_HOLDINGS_DESIGN.md` 11장)
- F14 주문 관리: 정정 API의 `quantity`가 "새 잔량"인지 "새 총 주문 수량"인지(화면은 잔량 입력, 어댑터가 변환), 체결 알림 기본값(제안: 앱 안 켬·Slack 끔) (`docs/ORDER_MANAGEMENT_DESIGN.md` 8장)
